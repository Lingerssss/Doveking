public class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;
    TreeNode(int x) {
        val = x;
    }
    /*TreeNode(int[] p){
        for(int i=0;i<p.length;i++){
            TreeNode root=new TreeNode(p[i]);
            root.left=new TreeNode(p[i+1]);
            root.right=new TreeNode(p[i+2]);
        }
    }*/
}
